export interface MeInfo {
    permission: string
    branches: number[]
}

export interface VendaBrutaRow {
    DATA: string
    IDEMPRESA: number
    NOME_EMPRESA: string
    VALOR_VENDA_BRUTA: number
}
